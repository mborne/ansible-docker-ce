# /etc/docker/daemon.json parameters
docker_data_root: '/var/lib/docker'
docker_storage_driver: 'overlay2'

docker_networks:
  - { name: 'webgateway', subnet: '192.168.150.1/24' }

# /usr/local/bin/docker-compose version
docker_compose_version: '1.24.0'
